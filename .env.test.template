# Copy this file to .env.test and update values for your local test database
# IMPORTANT: In CI (GitHub Actions), DATABASE_URL is provided by the PostgreSQL service container.
# The .env.test file is only used as a fallback when DATABASE_URL is not already set.

# Example PostgreSQL test DB URL
# Ensure the database exists and the user has privileges to reset it.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/property_management_test
JWT_SECRET=test_jwt_secret
NODE_ENV=test
